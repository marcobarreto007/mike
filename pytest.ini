[pytest]
testpaths =
    tests/unit
addopts = -ra
markers =
    integration: exercises multiple Mike components without requiring the live runtime
    runtime: requires Mike and/or Qwen to be listening on their configured ports
    e2e: exercises a complete live Mike workflow
