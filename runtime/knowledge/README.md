# Mike Knowledge Dropzone

Put `.md`, `.txt`, `.json`, `.jsonl`, `.yaml`, or `.yml` files here.

Mike indexes this folder into the local RAG store on startup and when
`POST /v1/knowledge/reindex` is called.

Suggested uses:
- family notes and preferences
- project docs you want Mike to know
- roadmap snippets
- operating procedures
